import { KeybindsDirective } from './keybinds.directive';

describe('KeybindsDirective', () => {
  it('should create an instance', () => {
    const directive = new KeybindsDirective();
    expect(directive).toBeTruthy();
  });
});
